App\Model\OAuth2Request:
  exclusion_policy: ALL
  properties:
    username:
      expose: true
      type: string
      groups: [ 'Create', 'Show' ]
    password:
      expose: true
      type: string
      groups: [ 'Create', 'Show' ]
    grantType:
      expose: true
      type: string
      groups: [ 'Create', 'Show' ]
